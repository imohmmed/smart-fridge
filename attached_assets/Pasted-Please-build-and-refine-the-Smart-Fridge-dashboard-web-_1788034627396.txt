Please build and refine the 'Smart Fridge' dashboard web application based on a modern design with a warm color palette (light beige background, sage green accents). Implement the following layout, UI/UX, and functional requirements:
1. Navigation Menu (Floating Vertical Dock): Completely redesign the sidebar into a 'Floating Vertical Dock'. The container should be a dark, heavily rounded pill-shape floating on the side. Inside it, stack the navigation icons vertically. Style the icons as 'squircles' (squares with highly rounded corners) featuring solid, vibrant background colors and simple white icons inside, mimicking the modern iOS or Telegram settings style.
2. Mobile Sidebar Overlay & Animation (Crucial Fix): On mobile, this dock/sidebar must behave strictly as an 'Off-canvas Overlay'. Set it to position: fixed; with a high z-index so it floats over the main content with a semi-transparent backdrop. It must NEVER push or shift the main page content. The opening/closing animation must be flawlessly smooth and lightweight with zero lag. Do not put a language toggle in this menu (move it to the Settings page).
3. Main Central Area (The Fridge): Display an open fridge with distinct categorized shelves (Dairy, Proteins, Vegetables, etc.). For the food items, remove the text indicating quantity (e.g., 'X units') below the names. Keep only a notification-style number badge floating on top of the food icon for a cleaner look.
4. Bottom Statistics Area: The bottom bar should not be one large stretched container. Please separate the statistics (Water intake, Calories remaining, Nutrients progress bars, and Daily tip) into distinct, individual floating cards/modules with proper padding and spacing.
5. Right Panel: Maintain the dynamic cards: an 'Item Details' card (image, expiry date, total calories, action buttons) and a 'Shopping List' card.
Ensure the code prioritizes a bug-free responsive grid, smooth animations, and a clean, uncluttered UI on both desktop and mobile.